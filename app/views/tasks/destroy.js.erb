$(document).ready(function() {
  $('.remove-task-btn').on("ajax:success", function(e) {
    $(e.currentTarget).parent().hide();
  });
});

var time = <%= @time %>
var newTimeLeft = Math.ceil(Number(document.getElementById('timer').innerHTML.split(":")[0]) - time)
console.log(newTimeLeft);
document.getElementById('timer').innerHTML = newTimeLeft + ":" + "00";

// // countdown timer
//       var cD = <%= Task.sum(:time) %>
//       var pageLoadTime = new Date();
//
//         document.getElementById('timer').innerHTML =
//         cD + ":" + 00;
//         startTimer();
//
//       function startTimer() {
//         var presentTime = document.getElementById('timer').innerHTML;
//         var timeArray = presentTime.split(/[:]+/);
//         var m = timeArray[0];
//         var s = checkSecond((timeArray[1] - 1));
//         if(s==59){m=m-1}
//         // if(m<0){alert('timer completed')}
//
//         document.getElementById('timer').innerHTML =
//           m + ":" + s;
//         setTimeout(startTimer, 1000);
//       }
//
//
//       function checkSecond(sec) {
//         if (sec < 10 && sec >= 0) {sec = "0" + sec}; // add zero in front of numbers < 10
//         if (sec < 0) {sec = "59"};
//         return sec;
//       }
